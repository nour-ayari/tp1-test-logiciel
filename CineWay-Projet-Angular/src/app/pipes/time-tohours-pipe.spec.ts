import { TimeTohoursPipe } from './time-tohours-pipe';

describe('TimeTohoursPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeTohoursPipe();
    expect(pipe).toBeTruthy();
  });
});
